import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "react-hot-toast";
import Navigation from "@/components/Navigation";
import { SessionManagerProvider } from "@/components/SessionManagerProvider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FindMe - Connect Talent with Opportunity",
  description: "A culture-first job matching platform connecting job seekers and companies based on values and compatibility.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <SessionManagerProvider>
          <Navigation />
          {children}
          <Toaster />
        </SessionManagerProvider>
      </body>
    </html>
  );
}
